export type LongOrders = {
    id: number;
    businessUnit: string;
    cardType: string;
    cardNumber: string;
    startDate: string;
    endDate: string;
    serviceType: string;
    hourInterval: string;
    source: string;
    address: string;
    customer: string;
}